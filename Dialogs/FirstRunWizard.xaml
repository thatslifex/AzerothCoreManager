<ui:FluentWindow x:Class="AzerothCoreManager.Dialogs.FirstRunWizard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Welcome to AzerothCore Manager"
        Width="700" Height="550"
        MinWidth="600" MinHeight="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Welcome to AzerothCore Manager"
                   FontSize="22" FontWeight="SemiBold" Foreground="White"
                   Margin="0,0,0,8" />

        <TextBlock Grid.Row="1" Text="This wizard will guide you through setting up your AzerothCore server. You can skip any step and run it later from the Setup page."
                   Foreground="#FFAAAAAA" FontSize="13"
                   TextWrapping="Wrap" Margin="0,0,0,20" />

        <!-- Step Content Area -->
        <Border Grid.Row="2" Background="#CC1A1A2E" CornerRadius="6" Padding="16">
            <StackPanel>
                <TextBlock x:Name="StepTitle" Text="Step 1/6: System Check"
                           FontSize="16" FontWeight="SemiBold" Foreground="White"
                           Margin="0,0,0,12" />

                <TextBlock x:Name="StepDescription"
                           Text="Checking your system for required tools..."
                           Foreground="#FFAAAAAA" FontSize="13"
                           TextWrapping="Wrap" Margin="0,0,0,12" />

                <!-- Prerequisites list (placeholder) -->
                <ItemsControl x:Name="PrerequisitesList" Margin="0,0,0,12">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="0,2">
                                <TextBlock Text="•" Foreground="#FF888888" Width="16" />
                                <TextBlock Text="{Binding}" Foreground="#FFCCCCCC" FontSize="12" />
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Progress Bar -->
                <ProgressBar x:Name="StepProgress" Height="6" IsIndeterminate="False"
                             Minimum="0" Maximum="100" Value="0"
                             Margin="0,8,0,0" />
            </StackPanel>
        </Border>

        <!-- Navigation Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button x:Name="SkipButton" Content="Skip" Width="80" Height="32"
                    Margin="0,0,8,0" Click="SkipButton_Click" />
            <Button x:Name="BackButton" Content="Back" Width="80" Height="32"
                    Margin="0,0,8,0" Click="BackButton_Click" IsEnabled="False" />
            <Button x:Name="NextButton" Content="Next" Width="80" Height="32"
                    Click="NextButton_Click" />
        </StackPanel>

        <!-- Step Indicators -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center"
                    Margin="0,12,0,0">
            <TextBlock x:Name="StepIndicator" Text="● ○ ○ ○ ○ ○"
                       Foreground="#FF888888" FontSize="14"
                       FontFamily="Segoe UI Symbol" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>
